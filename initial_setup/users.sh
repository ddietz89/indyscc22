# List out your users here
# Run "id <user>" to find the UID. It's critical all the users on eaech of the nodes has a matching UID.
# In a real system you'd have some central source for these like LDAP.


useradd -m -u 1500 ddietz
